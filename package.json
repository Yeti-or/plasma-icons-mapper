{
  "name": "plasma-icons-mapper",
  "private": true,
  "workspaces": [
    "packages/*",
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w web\"",
    "dev:server": "npm run dev -w server",
    "dev:web": "npm run dev -w web",
    "build": "npm run build -w @plasma-icons-mapper/shared && npm run build -w server && npm run build -w web",
    "test": "npm run test -w server",
    "typecheck": "npm run typecheck -w @plasma-icons-mapper/shared && npm run typecheck -w server && npm run typecheck -w web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
